apiVersion: v1
kind: ServiceAccount
metadata:
  name: ms-aws-eks-demo
  namespace: ms-aws-eks-demo
  labels:
    app.kubernetes.io/name: ms-aws-eks-demo
  # When the app starts calling AWS APIs, attach an IAM role via IRSA:
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/ms-aws-eks-demo
